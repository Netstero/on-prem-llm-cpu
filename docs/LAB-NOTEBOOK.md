# Lab notebook

> The raw, append-only build log for this project — newest entry first. It is published unedited (only
> infrastructure identifiers were scrubbed) to show the honest process, including wrong turns and the bug
> that produced one invalid benchmark cell. It occasionally references internal-only working docs
> (`plan.md`, `CLAUDE.md`) that are not part of this public repo, and mechanism names (an SSH wrapper,
> a CT) specific to the author's setup. Format: `## YYYY-MM-DD HH:MM — title`.


## 2026-06-25 20:46 — Qwen sampling-override defect found + corrected (docs + harness)
Inspecting `bench-payloads.py` (prompted by "are the prompts in the public repo?"), found `_payload()` hard-codes `temperature=1.0, top_p=1.0, top_k=0, min_p=0` into EVERY request body. llama.cpp honors request-body sampling OVER the server `--temp` flags, so the Qwen benchmark actually sampled at temp 1.0 — NOT the intended 0.7/0.8/20 set on the server; only `repeat_penalty 1.05` (absent from the body) applied. gpt-oss unaffected (intended sampling == body values). VERIFIED 3 ways: server `/props` shows the 0.7 defaults; saved request bodies contain 1.0; greedy probe (`temperature:0,top_k:1`) → byte-identical output while no-body-sampling requests vary → body overrides server. IMPACT: speed metrics sampling-independent → all speed figures unchanged; only the summarization quality result (3/8) is implicated (ran hotter than intended).
FIXED: corrected the Qwen sampling statement + added a limitations item across all result docs (qwen3-coder-30b.md §2.3/§6.2/§6.4; CASE-STUDY.md §2.3.2/§5.5/§8 item 11); fixed `bench-payloads.py` to leave sampling out of the body so server flags govern future runs. Speed tables untouched; cross-refs verified.

## 2026-06-19 14:05 — CASE-STUDY.md grounding verification (anti-hallucination pass)
Per user, verified every fact against ground truth. Recomputed ALL medians from both CSVs (pulled Qwen CSV local to results/20260618-220001/) → every gpt-oss §4 / Qwen §5 / head-to-head §6 table value matches exactly. Methodology verified vs scripts (grid 28 cells×3=84; cold/warm protocol; fixtures 4417/53/8-figures; single-instance+preflight; 3-reps-median; per-model sampling). Build flags verified vs CMakeCache.txt: ik GGML_NATIVE/AVX2/FMA=ON AVX512=OFF; mainline GGML_NATIVE=ON (AVX2/FMA via native autodetect) — matches §2.4. Model specs verified vs GGUF ground truth (both run server.logs): gpt-oss arch=gpt-oss 24 layers/32 experts/top-4/20.915B/ctx131072; Qwen qwen3moe 48/128/top-8/30.532B/ctx262144/GQA32-4. Flags: gpt-oss 0 flagged/1 nonpass (the known false-pos); Qwen 0 flagged/12 nonpass (6 summ + 6 main-30k overflow) — matches doc.
FOUND + FIXED 2 real errors + tightened 2 generalizations: (1) "warm TTFT ~0.06–0.37s regardless of depth" was FALSE — true for ik (≤0.14s) but gpt-oss MAINLINE warm hits 1.56s @30k-long / 0.68s @16k-long; corrected in §1.6/§7.1/§10. (2) "Qwen tokenizes ~10–15% higher" was FALSE as a blanket — only at deep cells (16k +12%, 30k +13%); at the 600 cell Qwen is LOWER (599 vs 638), 4k only +6.7%; corrected §3.2/§5/§6. No other ungrounded claim; the one estimated figure (DDR4 ~55–60 GB/s) stays explicitly marked "estimated, not measured". gpt-oss-20b.md + qwen3-coder-30b.md untouched.

## 2026-06-19 13:30 — CASE-STUDY.md authored (combined two-model report)
Per user: combine the two studies into one comprehensive report WITHOUT changing/removing the individual RESULTS files. Created CASE-STUDY.md — shared hardware/CT/engines/methodology written once, both result sets (gpt-oss §4, Qwen §5 incl. the 30k-overflow caveat), a head-to-head §6, merged limitations §8, reproducibility §9, conclusion. All numbers verbatim from the two source docs / CSVs (no re-estimation). Cross-model headlines: Qwen decodes faster at short/mid ctx (ik ~22 vs ~13.6 t/s), gpt-oss prefills better at depth (16k cold TTFT ~308s vs ~697s) → decode crossover ~16k; ik is the engine of choice for both; depths not token-identical (different tokenizers, flagged). Noted: same summ grader passed gpt-oss 8/8 but Qwen 3/8 → grader is functional, Qwen result consistent with omitted/reformatted figures (not disambiguated). gpt-oss-20b.md + qwen3-coder-30b.md left untouched (authoritative sources); RESULTS-BOTH indexed in CLAUDE.md.

## 2026-06-19 12:55 — qwen3-coder-30b.md authored (publication-style)
Wrote qwen3-coder-30b.md from run 20260618-220001, mirroring gpt-oss-20b.md structure. Grounded strictly: model facts read from the loaded GGUF (server.log: qwen3moe, 30.532B params, 48 layers, 128 experts/8 used, GQA 32/4 heads, head_dim 128, n_embd 2048, n_ctx_train 262144, ftype Q4_K-Medium, size 16.447 GiB / 17,665,334,432 B on disk byte-exact); engines from config commits (ik 670a3f6, main 6e14286ed/b9631); all speed/quality numbers = medians of 3 reps from results.csv. gpt-oss comparison numbers pulled from its real CSV (20260614-191322), with the explicit caveat that the two tokenizers give non-identical token depths per cell (Qwen ~10-15% higher). 30k cells documented as INVALID (overflow). Methodology section kept thin, references gpt-oss-20b.md §3/METHODOLOGY.md (SSOT — no duplication). Indexed in CLAUDE.md + plan.md RESUME HERE. Did NOT run the deep 64k re-run (user: enough results for now; confirming Qwen-slow-at-64k not worth ~30min/cell).

## 2026-06-19 12:33 — Qwen full grid COMPLETE (84/84) + critical context-overflow finding
Scheduled run fired 22:00 (one-shot systemd-run timer qwen-bench, ran as claude); finished ~10:30, auto-restored gpt-oss.service (active+HEALTHY, serving Qwen). RUN_ID=20260618-220001 (results/). 84/84 rows, no harness flags. Transient timer units cleaned (stop + reset-failed).
CRITICAL FINDING — the "30k" cells are INVALID for Qwen: the fixtures were section-count-calibrated to gpt-oss's tokenizer (~30k tok, fit 32768). Qwen's tokenizer expands the same filler ~15% → the 30k-cell prompt = 34483 tokens, OVER the 32768 ctx. Engines diverged: mainline correctly REJECTED (HTTP 400 exceed_context_size → 6 honest FAIL rows, spd_main_30k_*); ik SILENTLY truncated/context-shifted and reported prompt_tokens≈18099 (not 34483) → plausible PASS rows that are NOT a real 30k decode (the tell: ik "30k" ptok 18.1k ≈ its "16k" 17.9k). So ALL 30k cells (ik+ikfa+main) are unusable; deepest VALID Qwen point = the "16k" cell (~18k tok). Harness gap: bias detector catches suspicious-fast cold TTFT but NOT context overflow / silent truncation — add an overflow guard before any re-run.
VALID DATA (≤16k, all PASS, no flags). Decode t/s (short/long): ik 600=22/17, 4k=17.4/13.0, 16k=9.0/6.9; main 600=21/15, 4k=11.5/8.7, 16k=3.4/2.5; ikfa 16k=9.2/6.8. Prefill t/s: ik 98/65/26 vs main 58/41/19 (600/4k/16k). Cold TTFT@16k ~12min (ik) / ~16min (main); warm ~0.14s. VERDICT: ik wins Qwen at EVERY depth (16k decode 2.6× main), gap widens with depth — cleaner than gpt-oss (where main won short chat). Qwen decode actually BEATS gpt-oss short-ctx (ik ~22 vs ~14) — research's 5-11 t/s estimate was too pessimistic. flash-attn (ikfa) ≈ ik (no help), consistent w/ gpt-oss.
QUALITY: summ task FAILs both engines (3/8 figures, score 0.375, systematic across reps) — likely grader figure-matching is gpt-oss-calibrated / Qwen formats numbers differently; needle+reason+code all PASS. NEXT (gated): (1) re-run 30k cells with Qwen-tokenizer-calibrated sections (land ~30k tok <32k); (2) check summ grader vs raw Qwen output; (3) then bench-report + gpt-oss-vs-Qwen comparison writeup. gpt-oss data untouched at 20260614-191322.

## 2026-06-18 20:41 — Qwen harness wired + smoke PASSED; full grid still pending
Wired the benchmark harness to A/B Qwen on the SAME suite as gpt-oss, reusing the ACTIVE_MODEL toggle: bench-suite.sh now selects model file + sampling + `--repeat-penalty` from ACTIVE_MODEL (mirrors 30-systemd.sh), and every CSV row carries a new `model` column (bench-report.py prints the model + reads by name, so old gpt-oss CSV still parses). Synced + bash -n/ast clean.
SMOKE (SMOKE=1, ik/600/1rep) ran on CT and PASSED both cells — pipeline fully validated for Qwen. Artifacts: results/20260617-192654/. First harness Qwen numbers (ik @600 tok): prefill ~97 t/s, decode 21.9 (short)/16.6 (long) t/s, cold TTFT ~6.3s → warm 0.06s; correct 2/2, no flags. (Sanity: matches the earlier live single-shot ~24 t/s short decode.)
Process note: the smoke was launched, the tool was rejected, and the client then shut down — but the nohup'd run survived the SSH drop, completed the 2-cell smoke, and auto-restored the service. Repeated "BENCH RUNNING" alarms during cleanup were pgrep/proc SELF-MATCHES (the check command's own cmdline contains "bench-suite"); /proc inspection confirmed NO real bench process. End state CLEAN: gpt-oss.service active+healthy serving Qwen (PID 92, 0.0.0.0:8080), no stray procs.
NEXT (gated): launch the FULL Qwen grid — `RDIR auto-dated; nohup bash ~/gpt-oss/scripts/bench-suite.sh >> ~/gpt-oss/results/suite.out 2>&1 &` (preflight stops the live service for the duration, restores at end). Hold THREADS=11/CTX=32768/cells/tasks/reps for apples-to-apples vs gpt-oss 20260614-191322. ~9-11h; then bench-report + a combined gpt-oss-vs-Qwen comparison.

## 2026-06-17 19:21 — Live service switched to Qwen3-Coder (single-service swap)
User: "switch to Qwen Coder for now." Box can't hold both resident yet (CT cap ~31 GB), so this is a swap not a coexist. Added a reversible SSOT toggle `ACTIVE_MODEL=gptoss|qwen` to config.env; taught 30-systemd.sh to pick model file + sampling from it (qwen → QWEN_* vars + `--repeat-penalty 1.05`; gptoss → primary vars, no rep penalty) and set the unit Description accordingly. Reuses the existing gpt-oss.service unit (no enable/disable churn). Switch back = set ACTIVE_MODEL=gptoss, re-sync, re-run 30-systemd.sh.
Verified idle (load 1.45, nothing mid-inference) before the restart. Ran 30-systemd.sh: healthy on :8080 in ~10s. /v1/models confirms Qwen (n_params 30.5B, n_ctx_train 262144, max_model_len 32768). Functional gen correct (fib lambda). QUICK LIVE TIMING @ ~52 tok ctx (single request, not the harness): prefill 63.7 tok/s, decode **24.3 tok/s** — markedly better than Sonnet's conservative 5–11 t/s research estimate, and even above gpt-oss's ~14–18 short-ctx decode. Caveat: trivial context; will fall with depth — proper characterization still needs the grid (gated on user "go for tests").

## 2026-06-16 21:22 — Qwen3-Coder-30B-A3B downloaded + installed (2nd model, benchmark target)
User cleared adding Qwen3-Coder-30B-A3B as a second model to A/B vs gpt-oss on the same harness. Sonnet research (this session) settled the choice: Apache-2.0 MoE 30.5B total / 3.3B active, 128 experts top-8, 256K native ctx; ik_llama.cpp + mainline both support `qwen3moe`; use Unsloth GGUF for its patched XML→OpenAI tool-call template. Pre-reqs done by USER (host-side): CT RAM cap 24→~31 GB, root disk 30→59 GB (was the real blocker — 18 GB model vs 15 GB free).
Picked quant UD-Q4_K_XL (Unsloth Dynamic 4-bit, 17.7 GB) over plain Q4_K_M (18.6 GB): smaller + better quality. Downloaded to /opt/models via background resume-capable curl (~42 MB/s, ~7 min). INTEGRITY: local size 17665334432 bytes == HF canonical LFS size, byte-exact; log clean, no curl left running (verified via /proc, defeating the pgrep self-match trap that twice gave false "STILL-RUNNING"). gpt-oss.service stayed active throughout — untouched.
Recorded as SEPARATE `QWEN_*` vars in config.env (model file/repo + Qwen's recommended sampling temp0.7/top_p0.8/top_k20 + repeat_penalty1.05) so the live gpt-oss SSOT is NOT altered; synced to CT. NOT YET DONE (gated on user "go for tests"): load-verify (needs brief gpt-oss stop — RAM won't hold both at once), harness model-switch wiring (MODEL_FILE/sampling selector + repeat-penalty knob + CSV `model` column), then smoke + grid as a new RUN_ID. Plan: hold THREADS=11/CTX=32768/cells/tasks/reps constant for apples-to-apples; each model at its own recommended sampling.

## 2026-06-16 17:05 — gpt-oss-20b.md grounding verification (anti-hallucination pass)
Re-derived every per-cell median from results.csv programmatically and compared to gpt-oss-20b.md tables: ALL §4/§4.5 numbers match exactly; all derived ratios verified (cold/warm 8555x/3082x/632x/97x; prefill ik/main 1.663/1.678/1.377/1.016; decode main/ik 600 1.224, ik/main 16k 1.535, 30k 2.966). Spec/research claims trace to model card + config.env + WORKLOG + verified research.
FOUND + FIXED one error: §2.4 claimed BOTH engines built with -DGGML_NATIVE/AVX2/FMA; true only for ik. Mainline 10-build-llamacpp.sh actually uses `cmake -DCMAKE_BUILD_TYPE=Release -DLLAMA_CURL=ON` (relies on llama.cpp default native-arch autodetect). Corrected to state each engine's real flags.
Also qualified the one estimated figure: DDR4 "~55-60 GB/s effective" now marked "(estimated, not measured)" with theoretical ~76.8 GB/s. No other ungrounded claims found; no unverified cost/price figure present.

## 2026-06-16 16:40 — Code-grader false-positive CLOSED (won't fix)
User decision: treat qual_main_code rep3 as a false-positive, document, and close — do NOT change the verifier. Confirmed it affects no performance figure (only that one cell's pass/fail; effective correctness 84/84). RESULTS §6.4 reworded from "logged for fix" to "accepted/left unchanged"; plan.md marks it CLOSED. Case closed.

## 2026-06-16 16:20 — Full benchmark grid COMPLETE + gpt-oss-20b.md authored
Suite finished: RUN_ID 20260614-191322, 84/84 runs captured (28 cells x 3 reps), service auto-restored, ZERO thermal throttling the entire run (package_throttle_count=0; idle 49C, peak ~77C). Artifacts: ~14 MB, 419 raw files (cold+warm req/resp + wall per run) under results/20260614-191322/.
HEADLINE RESULTS (medians, exact): cold->warm TTFT at 30k ik = 1026.6s -> 0.12s (~8550x). Engine A/B: ik prefill 1.66-1.68x faster at <=4k, converging to ~1.0x by 30k (both ~29 t/s, bandwidth-bound). DECODE: mainline wins short chat (600: 16.6 vs ik 13.6) but COLLAPSES with depth (30k: main 2.94 vs ik 8.72 -> ik ~3x). Flash-attn (ikfa vs ik @16k/30k): no effect (within noise) -> -fa hypothesis settled negative for ik. Quality: both engines correct on needle/summ(8/8 figures)/reason(53)/code; effective 84/84.
DATA-INTEGRITY FINDING: the one recorded FAIL (qual_main_code rep3) is a GRADER FALSE POSITIVE, not a model error. Model produced the correct stack algorithm; its docstring contained the word "input", which bench-verify.py's safety denylist (\binput\b) matched (the regex scans comments/strings, not just calls) -> "unsafe-code-skipped". RECOMMENDED FIX (pending): strip comments/docstrings before the denylist scan, or narrow the denylist to call-shaped patterns. Also: 1 warm re-send missing (ikfa_30k_short rep3 warm fields blank) -> 83/84 warm samples; not flagged.
Authored gpt-oss-20b.md as a publication-style report: methodology + metric definitions + quality verification + explicit comparison of our approach to industry best practice (MLPerf Inference, vLLM bench, llama-bench, NVIDIA GenAI-Perf, LLMPerf; NIAH/RULER/HumanEval/pass@k; goodput/percentiles). Best practices cross-verified across multiple sources via Sonnet research subagents; gpt-oss specs verified against the OpenAI model card (arXiv 2508.10925) + HF config.json. All numbers in gpt-oss-20b.md are taken verbatim from results.csv; nothing estimated.

## 2026-06-14 11:38 — Phase 0: access wrapper
Created gpt-oss/{px,.askpass.sh} (askpass reuses proxmox-test/claude-secrets — one passphrase source). px sources scripts/config.env for target/key. Verified: `./px` → OK, passwordless sudo → SUDO_OK. Note: CT hostname is "lllm" (triple-l, likely create-time typo); we connect by IP so non-blocking.

## 2026-06-14 14:49 — Verify CT + fix hostname
Recon: Debian 13 trixie, 12 cores, AVX2 present, 24G RAM, 28G free on /dev/vg--nvme (nvme), DNS ok. Build deps (gcc/git/cmake) NOT yet installed. Hostname was "lllm" (create typo) → corrected: `hostnamectl set-hostname llm` + /etc/hosts. Now `hostname`=llm. Access by IP unaffected throughout.

## 2026-06-14 14:51 — Phase 1: build deps (00-deps.sh)
Ran scripts/00-deps.sh on CT (synced via tar-over-ssh to ~/gpt-oss). Installed build-essential, git, cmake, libcurl4-openssl-dev, libssl-dev, pkg-config, curl. Result: gcc=14, cmake=3.31.6, git=2.47.3. Sanity passed (AVX2, nproc=12, RAM=24G). Cosmetic: SSH forwards LC_TIME=pl_PL.UTF-8 not generated on CT → locale warnings, non-blocking.

## 2026-06-14 14:55 — Phase 2+3: build llama.cpp + fetch model (parallel)
Build (10-build-llamacpp.sh) and download (20-fetch-model.sh) run concurrently (CPU-bound vs network-bound). FAILURE+FIX: first download 404d — config had MODEL_FILE=gpt-oss-20b-MXFP4.gguf but HF repo uses lowercase gpt-oss-20b-mxfp4.gguf (found via HF API). Fixed once in config.env (SSOT) → re-synced → re-downloading at ~38 MB/s. Note: background "exit 0" was the trailing echo masking the script failure; always check the log, not just exit code.

## 2026-06-14 15:01 — Phase 4+5: serve + verify (COMPLETE)
build: llama.cpp commit 6e14286ed (llama-server b9631), pinned in config.env LLAMA_COMMIT. model: gpt-oss-20b-mxfp4.gguf 11548 MB. 30-systemd.sh installed gpt-oss.service (enabled+active), healthy in ~15s. E2E: /v1/chat/completions returns coherent text. PERF: ~17 tok/s (far above the conservative 3-6 estimate — MoE 3.6B active params wins). Reachable off-box at http://<CT_IP>:8080. RSS 10.4 GB; host 10/24 GB used. Blueprint scripts (00->30 + install.sh) reproduce from scratch. DONE.

## 2026-06-14 15:09 — CPU pinning verified (user applied host-side)
User set lxc.cgroup2.cpuset.cpus on the Proxmox host. Verified from inside CT: cpuset.cpus.effective=0-11; shell + llama-server Cpus_allowed_list=0-11. Sibling check: cpu0..11 each pair with a sibling in 14-25 → 0-11 are 12 DISTINCT physical cores, no HT collision. Re-benchmark: ~18.3 tok/s (vs ~17 unpinned). Pinning is now deterministic; cores 12-13 + siblings 14-27 left for L0/lab.

## 2026-06-14 15:20 — Incident: RAM thrash during benchmarking + recovery
INCIDENT: ran llama-bench sweep AND a llama-cli SIMD probe (both load full model) while gpt-oss.service was running → 3 model working sets vs 24G cap. Model files are clean mmap pages so kernel thrashed (evict/re-read) instead of OOM-killing; sshd became unreachable. RECOVERY: ssh sessions timed out → SIGHUP killed the transient loaders → pressure cleared; user also did pct stop/start <CTID>. Service auto-started on boot (validates enable). Post: mem 139M used/23G avail, swap 0, cpuset pinning 0-11 persisted (in CT conf), health ok.
LESSONS: (1) never benchmark against the running service — stop it, sweep, restart; (2) never load the model just to read build flags (--version prints without -m).
CORRECTION to earlier "fix hostname" entry: in-CT hostnamectl does NOT persist — Proxmox re-applies hostname from CT config (still lllm) on every boot. Real fix is host-side: `pct set <CTID> --hostname llm`.

## 2026-06-14 15:23 — Service disabled for stress-testing
`systemctl disable --now gpt-oss` → disabled + inactive, model unloaded (mem 28M used / 23G avail). Done so benchmarks run as the sole model process (avoids the earlier thrash). Re-enable after: `sudo systemctl enable --now gpt-oss`.

## 2026-06-14 15:32 — Thread sweep (clean, service stopped) — RESULT
llama-bench, single process, cpuset 0-11, 3 reps. Decode tg128: 6t=14.08, 8t=17.21, 10t=19.14, 12t=20.13 t/s. Prefill pp512: 6t=29.2, 8t=38.1, 10t=47.1, 12t=55.8. CONCLUSION: 12 threads optimal (current config correct); decode near-saturated (~5% gain 10->12, still climbing); ~20 t/s is the pure-decode ceiling (live ~18 = +template/sampling overhead). Expanding to 0-13 (14 cores) would add ~1 t/s decode but starve L0 host — NOT recommended. No config change. Box is at practical max.

## 2026-06-14 16:35 — Long-context test (ik_llama.cpp, 21k tokens) — RESULTS
Needle-in-haystack via ik llama-server (ctx 32768, -fa 1, -t 11 on cores 0-10, core 11 reserved for sshd). Prompt 21351 tokens, planted fact at ~55% depth.
CORRECTNESS: model retrieved the needle (ZX-4417-QUILL, Section 386). Long context works.
MEMORY: peak RSS ~13.7 GB for 32k-ctx server w/ 21k prompt; ~10 GB headroom under 24 GB cap; swap 0. Long ctx NOT memory-bound.
SPEED at 21k depth: prefill ~74 t/s (=> ~4.8 min TTFT for 21k prompt; vs pp512=104); decode ~9.7 t/s (vs ~19 short — KV-attention ~halves decode at depth, fundamental to both engines).
IMPLICATION: ik prefill +87% halves long-prompt TTFT (~4.8 min ik vs ~9 min mainline est). For long-context use ik is clearly worth it; for short chat it is the invisible -5% decode.
OPS LESSONS: (1) -t 11 keeps a core for sshd, but idle/duplicate ik servers busy-spin cores 0-10 and still starve ssh; (2) nohup-launched servers survive even when launching ssh returns 255 -> caused duplicate instances; ALWAYS verify single instance + nohup the request too. Recovery via host-side `sudo pkill -9 -f llama-server`.

## 2026-06-14 18:27 — ik long-context characterization (post-reboot, monitored)
Clean CT reboot; ik service auto-started (enabled). Sweep driven from workstation via API (no CT sshd in the request path); CPU/RAM monitored via px every 25-30s.
MONITORING proved real execution: CPU ~92% busy (11/12 cores) throughout each prefill; RAM used ~1.8 GB / avail ~22.7 GB stable even at 30k ctx (KV pre-allocated, tiny). Earlier "0% CPU stall" was a dead curl connection; reboot fixed it.
ik results (prompt_tok | prefill t/s | decode t/s | needle): 638|88.4|13.8|YES ; 4104|78.5|12.7|YES ; 15960|49.8|10.9|YES ; 30554|~39 cold|8.7|YES(answered 4417).
Long-context retrieval correct at ALL depths incl near-full 32k. Memory non-issue with ik (mmap, lean) -> ~22 GB free for OpenWebUI.
GOTCHAS: (1) needle must be benign + exactly reproducible — "authorization code" triggered a safety REFUSAL; odd names ("Wobblefant") get normalized; jersey number 4417 works. (2) near-32k prefill ~13 min -> curl needs --max-time >900s. (3) px/sshd flaky under rapid reconnects; drive long requests from local curl, keep service at 11 threads so a core stays free for sshd.

## 2026-06-14 18:40 — [BACKFILL ~15:40] ik_llama.cpp build + A/B (bench + live)
Built ik_llama.cpp (CPU/MoE-optimized fork) in /opt/ik_llama.cpp, commit 670a3f6, -DGGML_NATIVE=ON -DGGML_AVX2=ON -DGGML_FMA=ON. Recorded in config.env (IK_DIR/IK_REPO/IK_COMMIT). Additive — mainline /opt/llama.cpp untouched (instant fallback).
llama-bench A/B (12t, same MXFP4 model): PREFILL pp512 ik 104.6 vs mainline 55.8 (+87%); DECODE tg128 ik 19.1 vs mainline 20.1 (-5%). ik MoE flags -fmoe/-rtr did NOT help (slightly worse). 
CRITICAL CORRECTION via LIVE A/B (systemd service, /v1/chat/completions, same prompt, cache-cold): bench understated live decode overhead. Live @3175-tok prompt: prefill ik ~92 vs mainline ~53 (ik ~1.7x); decode ik ~13.4 vs mainline ~13.7 (≈equal at depth). At SHORT prompt: mainline ~17-18 vs ik ~14 (mainline ~20% faster decode, NOT the bench's -5%). Net: ik wins prefill/TTFT + RAM; mainline wins short-chat decode. Engine choice = workload-dependent; decided to settle it with the full benchmark suite.

## 2026-06-14 18:41 — [BACKFILL ~17:30] Config tuning: threads/slots/flash-attn
Standing service config changed (config.env SSOT): THREADS 12 -> 11 (leave 1 of the 12 pinned cores for OS/sshd/pct-enter; user decision). SLOTS=1 via --parallel 1 — discovered llama-server defaulted to 4 parallel slots (each n_ctx=32768) but that did NOT multiply KV (KV stayed 1.5 GB total); set 1 for single-user. FA="" (flash attention OFF default): first thought -fa cost ~26% short-ctx decode, but re-test showed decode ~14 even without -fa -> the slowdown is LIVE-PATH overhead vs llama-bench, NOT -fa. -fa kept available as a knob for long-context (saves little here since KV already tiny). 30-systemd.sh updated to use $SERVE_BIN + $FA + --parallel $SLOTS.

## 2026-06-14 18:42 — [BACKFILL ~17:45] Memory truth (KV vs page-cache "double register")
Authoritative from load log: model 11.27 GiB, KV self size 1.5 GiB (the FULL 32k context window — cheap!), compute buffer 0.4 GiB. True committed footprint ~13.4 GiB.
The scary "10Gi used / 11Gi buff/cache / 2Gi free": the GGUF appears TWICE — once in the process + once as reclaimable page-cache of the file. Proven by stopping the server: with ZERO model procs, 11Gi buff/cache persisted (pure file cache). `available` (~13-22 GiB) is the real headroom metric, not `free`.
Engine memory differs: ik mmaps -> used ~1.7 GiB (model shared with cache); mainline made an anonymous copy -> used ~10 GiB. ik is the leaner engine -> ~22 GiB free for a future OpenWebUI (which only calls the API, doesn't load the model). KV ≠ context window cost on top; KV IS the context window storage.

## 2026-06-14 18:43 — [BACKFILL] Ops lessons from the A/B churn (SSH/cache/refusals)
(1) px/sshd returns 255 / hangs under rapid reconnects and during full-core inference -> drive long requests from LOCAL curl to the API (<CT_IP>:8080), keep service at 11 threads so a core stays free for sshd, and don't hammer reconnects. (2) Cache: a re-sent identical prompt is a cache HIT -> bogus prefill t/s (saw near-32k report 16 t/s cached vs ~39 cold). Measure cold: restart server before each measured run; warm-up must use a DIFFERENT throwaway prompt. (3) Refusals: "authorization code/secret" framing makes GPT-OSS refuse; odd proper names get normalized (Wobblefant->Wobble). Use benign + exactly-reproducible needles (jersey number 4417). (4) Background curl must be nohup'd or it dies by SIGHUP when its launching ssh closes; verify single server instance (pgrep self-matches inflate counts). (5) `sudo reboot` works from inside the CT (passwordless sudo) for a full clean restart; service auto-starts (enabled).

## 2026-06-14 20:08 — Benchmark harness built + locally validated
Built the case-study harness in scripts/ (all values from config.env SSOT; added REPS/OUT_SHORT/OUT_LONG/RESULTS_BASE there): bench-payloads.py (single owner of all prompts + fixtures), bench-verify.py (task correctness → PASS/FAIL+score; code task execs model's is_balanced vs 8 cases in a restricted sandbox with an import/os denylist), bench-suite.sh (orchestrator, runs ON the CT, fresh-server-per-run cold-cache methodology + different-prompt warm-up + local curl + raw artifact capture + resumable-by-RUN_ID), bench-report.py (median tables).
VALIDATED LOCALLY (no CT touched): bash -n clean; all 5 task payloads generate; verifier discriminates correctly — needle 4417 PASS vs missing FAIL, reason 53 PASS vs 41 FAIL, summ 8/8 PASS vs 2/8 FAIL, correct stack solution 8/8 PASS, count-only solution 6/8 FAIL, `import os` rejected as unsafe.
Synced to CT (tar-over-ssh). Live gpt-oss.service still running (untouched). NEXT: SMOKE=1 run, then full grid via nohup (stops the service for the duration, restores at end). Design recorded in plan.md "## Benchmark suite".

## 2026-06-14 21:08 — Harness v3: self-defending (answer to "do we need reboots?")
User worried an unattended overnight run could deliver nothing/biased without reboots. Questioned it critically: the project's real incidents were (a) RAM thrash from THREE concurrent model loads and (b) orphan llama-server from manual nohup-over-flaky-ssh — neither caused by "no reboot"; reboots were a RECOVERY hammer, not prevention. A fresh PROCESS per run already gives cold KV+prompt cache; killed procs release memory immediately; model in page cache doesn't bias token rates. Conclusion: reboots wrong fix (adds cold-reload cost + resume-after-reboot failure surface); right fix = make the harness self-defending. Added: (1) PREFLIGHT clean-slate gate — aborts whole suite if stray server / port busy / <15GB RAM (refuses to burn the night on a dirty box); (2) stop_server SIGTERM→SIGKILL escalation, before AND after each run; (3) single-instance assertion (clean+retry once, else flag) — kills the contention-bias mode; (4) memory-headroom check; (5) cold-cache bias detector (big prompt + sub-1s TTFT = accidental cache hit → flag); (6) `flags` CSV column + end-of-run "Data health" summary (rows vs expected, PASS count, every flagged/non-pass row). Resume now keys on the CSV row (written last) so an interrupted run re-does cleanly — reboots are now OPTIONAL, not load-bearing.
BUG found+fixed in smoke: `pgrep -fc` prints "0" AND exits 1 on no match, so `|| echo 0` doubled it ("0\n0") → every numeric guard errored. Fixed to capture stdout only; verified count_servers→clean "1" against live service (no self-match). Final smoke: preflight OK, guards evaluate, flags empty, data-health clean, service restored. Smoke result dirs cleaned. METHODOLOGY.md updated. NEXT: launch full grid.

## 2026-06-14 20:46 — Harness v2: cold+warm TTFT capture + ik -fa long-ctx arm; smoke OK
User questions drove two additions. (1) COLD+WARM: each run now re-sends the IDENTICAL prompt without restart (max_tokens=16) right after the cold measure → prefix cache HIT → records warm_prefill_tps/warm_ttft_s. Near-free (prefill≈0 + 16 tok); adds ~10-15 min over the whole grid, NOT a second pass. Clarified the warm-up uses a DIFFERENT prompt on purpose (keeps cold cold); warm is a separate same-prompt send. (2) -fa: prior "-fa costs 26% decode" was a red herring (live-path overhead, not -fa) and was only ever short-ctx; never tested at depth where flash-attn should help. Added an ik+`-fa` arm on long-ctx ONLY (16k+30k × short+long × 3 = 12 runs, ~2h) rather than crossing -fa with the whole grid (would be +72 wasted runs / full day). New engine variant `ikfa` (ik binary + `-fa 1`). Grid now ≈84 runs, ~9-11h.
VALIDATED: bash -n + py-block compile clean; cold/warm payloads share identical messages (true cache hit). Re-smoke on CT: ik@638 tok COLD TTFT 6.81s vs WARM TTFT 0.08s (~85x); both PASS; service restored healthy. CSV gained warm_prefill_tps/warm_ttft_s; report shows TTFT cold vs warm. METHODOLOGY.md updated (matrix, cold-vs-warm section, metrics, procedure, artifacts). NEXT: launch full grid via nohup.

## 2026-06-14 20:20 — Doc split: benchmark suite → METHODOLOGY.md
Per CLAUDE.md split-trigger (a phase grew its own deep design) and user request: benchmarking is a distinct task from building the hosting CT, so moved the whole "## Benchmark suite" section out of plan.md into a dedicated METHODOLOGY.md (matrix, prompt/metric rationale, methodology, how-to-run, decisions). plan.md now ends at "install done & serving" with a thin pointer to METHODOLOGY.md; RESUME HERE updated to point there. CLAUDE.md doc-suite index updated (added METHODOLOGY.md + gpt-oss-20b.md). No content duplicated — single owner per fact preserved (config.env=values, METHODOLOGY.md=benchmark rationale, scripts/bench-*=code, WORKLOG=history).
